SOURCE2METAL GITHUB UPLOAD PACKAGE v3.0.7
========================================

This ZIP is prepared for the public Source2Metal GitHub repository.
It does NOT modify Source2Metal v3.0.7 program logic.

It contains:
- public source trees for Source2Metal v3.0.7 and SyzygyCheck v2.0.7;
- the exact v3.0.7 source archive used as the embedded-source build input;
- build/reproducibility documentation;
- privacy and third-party notices;
- code-signing policy preparation.

Do not upload the outer ZIP itself as repository source.
Extract it first, then upload the CONTENTS of the extracted folder to the repository root.

The complete end-user distribution V3.0.7.zip will be added later as a GitHub RELEASE asset, not mixed into the repository source tree.
